import { describe, it } from 'vitest'

describe('useInvoices hook', () => {
  it.todo('createPendingInvoice stores image path and returns invoice id')
  it.todo('invoice record is created with status "pending"')
  it.todo('confirmInvoice updates invoice status to "confirmed"')
  it.todo('confirmInvoice calls updateIngredient for each matched line')
})
